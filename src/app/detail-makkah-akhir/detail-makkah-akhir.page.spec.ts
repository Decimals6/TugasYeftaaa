import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetailMakkahAkhirPage } from './detail-makkah-akhir.page';

describe('DetailMakkahAkhirPage', () => {
  let component: DetailMakkahAkhirPage;
  let fixture: ComponentFixture<DetailMakkahAkhirPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailMakkahAkhirPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
